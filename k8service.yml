
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: insurance
spec:
  replicas: 1
  selector:
    matchLabels:
      app: insurance
  template:
    metadata:
      labels:
        app: insurance
    spec:
      containers:
        - name: insurance
          image: snehaldhul/healthcare-image:latest
          ports:
            - containerPort: 8082
          env:
            - name: PORT
              value: "8082"
---
apiVersion: v1
kind: Service
metadata:
  name: insurance
spec:
  type: NodePort
  ports:
    - port: 8082
      targetPort: 8082
  selector:
    app: insurance
